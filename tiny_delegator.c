#include<stdio.h>
#include<stdlib.h>
#include<string.h>


int main() {
    int tmp = 0;
    for(int i = 0; i < 4; i++) {
        char *operation;
        char *x;
        char *y;
        fscanf(stdin, "%s %s %s",operation, x, y);
        if (strncmp(x,"%%",1) == 0) {
            
        }

    }
}